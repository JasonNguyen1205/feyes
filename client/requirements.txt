# Visual AOI Client - Python Dependencies

# Core dependencies
requests>=2.25.0

# Image Processing
opencv-python>=4.5.0
pillow>=8.0.0

# System monitoring
psutil>=5.8.0

# Flask (detected in imports)
flask>=2.0.0

# GTK (detected in imports) - MUST BE INSTALLED VIA SYSTEM PACKAGE MANAGER
# DO NOT install 'gi' via pip - it's Python 2 only and breaks Python 3.11+
# Ubuntu/Debian: sudo apt-get install python3-gi python3-gi-cairo gir1.2-gtk-3.0
# The system package provides the correct Python 3 bindings

# TIS Camera SDK (included in python/ directory - no pip install needed)
# Note: Requires system GI bindings for GObject introspection
